import { VarCanvasDirective } from './var-canvas.directive';

describe('VarCanvasDirective', () => {
  it('should create an instance', () => {
    const directive = new VarCanvasDirective();
    expect(directive).toBeTruthy();
  });
});
